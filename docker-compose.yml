services:
  vms-mysql-db:
    image: mysql:8.0
    container_name: vms-mysql-db
    restart: always
    environment:
      - MYSQL_ROOT_PASSWORD=123456
      - MYSQL_DATABASE=video_manage_system
    volumes:
      - vms-mysql-data:/var/lib/mysql
    ports:
      - "6932:3306"
    networks:
      - app-network

  vms-backend-va:
    build: .
    container_name: vms-backend-va
    environment:
      - VMS_VA_DATABASE_HOST=mysql-db
    ports:
      - "6590:6590"
    command: ./entrypoint.sh
    depends_on:
      - vms-mysql-db
    networks:
      - app-network

volumes:
  vms-mysql-data:

networks:
  app-network:
    driver: bridge